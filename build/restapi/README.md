This is a "RestApi", though you can call it from the browser.

Main functionality is to accept a parameter and use this to call the database.

This creates the two-tier application which we'll deploy in EKS.
